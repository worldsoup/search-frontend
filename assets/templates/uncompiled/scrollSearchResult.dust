{#.}
	{#hits}
		{#hits}
			{#_source} 
				<div class="results-list">
					<img class="inline" src="{appImages[0]}"/>
					<h4 class="inline" href="{#appStores}{Web}{/appStores}">{appTitle}</a></h4>
					<p>{appFullDescription}</p>
					<hr>
				</div>
			{/_source} 
			{:else}<p>No results found.</p>
		{/hits}
	{/hits}
{/.}